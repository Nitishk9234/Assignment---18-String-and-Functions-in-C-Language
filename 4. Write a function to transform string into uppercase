#include<stdio.h>
#include<string.h>
int main()
{
    char str[50];
    printf("Enter your name ");
    fgets(str,50,stdin);
    printf("String convert into uppercase %s",strupr(str));
    return 0;
}

#include<stdio.h>
#include<string.h>
int main()
{
    char str[20],l;
    printf("Enter your name");
    fgets(str,20,stdin);
    l=strlen(str);
    printf("length of %s is %d",str,l);
    return 0;
}
